{
  "name": "septa-consist-logger",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute septa-consists --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute septa-consists --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
